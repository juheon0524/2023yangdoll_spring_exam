Override => 상위에서 정의한 메서드를 재정의

생성자 => heap영역에 class의 구조대로 data를 만드는 역활 => 반드시 해야할 Action을 정의

인터페이스는 public만 사용가능 

final =>	1.class => 상속 X 부모class가 될수없다.
	2.변수 => static필드변수 = 필드 선언과 동시에 값을 반드시 초기화한다, 인스턴스필드변수 = 선언할때 초기화를 줘도 되고 생성자를 만들떄 줘도 된다, 지역변수 = 메서드 안에 정의된 변수  (만들어지는 시점에서 반드시 값의 초기화가 이루어져야한다.)

abstract(추상클래스) => 추상적 class : 상속을 강제  ,  메서드 : Override 강제

추상클래스 = 일반메서드 인스턴스메서드만으로 구성 될 수 있다.  class가 단 하나라도 추상메서드를 가지고 있다면 그 클래스는 반드시 추상클래스가 되어야한다. 메서드의 시그니서참 존재

모든 메서드가 추상메서드 => 인터페이스


-인터페이스는 추상메서드로만 이루어져 있다.
 => 자바 8(1.8)부터 2개의 메소드가 추가됨.(default, static) <= 추상클래스의 인스턴스 메서드를 많은 부분 대체하고 있다. => 오버라이딩이 안된다.
 => 추상클래스의 입지를 더욱 좁게 하고있다.
 => default 메서드가 만들어진 이유는
      인터페이스를 설계할 당시에 누락된 공통 기능을 추가해서 필요한 곳에서 사용할 수 있도록 부가적인 기능을 적용한 것.
 => 예전에 default가 없는 상태에서 사용하던 프로그램도 특별하게 수정없이 그대로 사용하고... 필요한 곳에서는 적용해서 새롭게 작성할 수 있도록...
 => static 메서드는 객체 생성과 무관하기 때문에 사용할 수 있었는데 오직 추상메서드로만 구성하려고 하다보니 제한을 했었다.
 => default 메서드를 추가함으로 인해서 제한할 필요가 없어졌다... 허용함