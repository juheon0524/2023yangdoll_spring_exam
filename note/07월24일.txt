문자열 => 숫자 - 1.paseInt => 단위포함 문자와 숫자중에서 숫자만 추출
	          2.Number() =>순수 숫자로만 이루어짐
	          3.문*1



XML 문서에 있는 내용을 가져올때  ---- 속성값 ----- 1. 문서의 내용 -----1-1 단순TEXT 1-2 태그가 포함된 내용(html).
				 입력값(val)
				 스타일 속성값(css)